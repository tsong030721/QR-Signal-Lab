import yfinance as yf
import pandas as pd

def fetch_one(symbol: str, start: str = None, end: str = None) -> pd.DataFrame:

def fetch_batch(symbols: list[str], start: str = None, end: str = None) -> pd.DataFrame:

def validate_params():
